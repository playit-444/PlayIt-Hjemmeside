export interface Game {
  description: string;
  gameTypeId: number;
  name: string;
  logoPath: string;
  coverPath: string;
  backgroundPath: string;
  maxPlayers: number;
  minimumPlayers: number;
}
